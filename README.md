# 🧬 Генетичний алгоритм для симуляції гри "Камінь–Ножиці–Папір"

## 🎯 Мета роботи

* Застосувати **генетичний алгоритм** для еволюційного пошуку оптимальної стратегії у грі “Камінь–Ножиці–Папір”.
* Продемонструвати, як через **відбір**, **схрещування** та **мутацію** популяція агентів поступово адаптується до середовища.

---

## 🧩 Опис моделі

Кожен агент у популяції має власну **стратегію** — набір трьох імовірностей:

```
[p(камінь), p(папір), p(ножиці)]
```

Сума елементів завжди дорівнює `1`.
На початку всі стратегії випадкові (розподіл Діріхле), а з поколіннями вони еволюціонують у напрямку більш виграшних комбінацій.

---

## 🧠 Основні етапи алгоритму

### 1️⃣ Ініціалізація

Створюється початкова **популяція агентів** розміром `POP_SIZE`.
Кожен агент отримує випадкову стратегію, що задає імовірності вибору кожного ходу.

---

### 2️⃣ Оцінка (Evaluation)

Кожен агент грає `MATCHES_PER_AGENT` партій проти випадково вибраних суперників.

Результати оцінюються так:

* Перемога → `+1`
* Нічия → `0`
* Поразка → `-1`

Середнє значення цих результатів є **пристосованістю (fitness)** агента.

---

### 3️⃣ Відбір (Tournament Selection)

З популяції випадково обирається підмножина з `TOURNAMENT_SIZE` агентів.
Перемагає той, хто має **найвищий fitness**, і саме він стає **батьком** для наступного покоління.

---

### 4️⃣ Схрещування (Crossover)

Для створення нового агента використовується **арифметичне схрещування** двох батьків:

```
child = α * parent1 + (1 - α) * parent2
```

де `α` — випадкове число у діапазоні `[0, 1]`.
Після схрещування стратегія нормалізується, щоб сума імовірностей дорівнювала `1`.

---

### 5️⃣ Мутація (Mutation)

Із ймовірністю `MUTATION_RATE` до стратегії додається **невеликий випадковий шум**, що дозволяє уникнути застою у локальних мінімумах і підтримує різноманітність популяції.

---

### 6️⃣ Формування нового покоління

За допомогою повторення етапів відбору, схрещування та мутації створюється **нова популяція** розміром `POP_SIZE`.
Процес повторюється протягом `GENERATIONS` ітерацій.

---

## 📈 Результати та візуалізація

Після кожного покоління фіксуються:

* Середній **fitness** популяції
* Середня **стратегія** (ймовірності вибору кожного ходу)

### 🔹 Графік 1 — Еволюція Fitness

Показує, як змінюється середня успішність популяції з поколіннями.
Зазвичай видно стабілізацію на певному рівні, коли система досягає рівноваги.

### 🔹 Графік 2 — Еволюція Стратегій

Відображає, як середні ймовірності вибору ходів (“камінь”, “папір”, “ножиці”)
еволюціонують у часі, показуючи процес адаптації популяції.

---

## ⚙️ Параметри алгоритму

| Параметр            | Значення | Опис                                   |
| ------------------- | -------- | -------------------------------------- |
| `POP_SIZE`          | 100      | Розмір популяції                       |
| `GENERATIONS`       | 100      | Кількість поколінь                     |
| `MATCHES_PER_AGENT` | 30       | Кількість ігор кожного агента          |
| `MUTATION_RATE`     | 0.1      | Імовірність мутації                    |
| `TOURNAMENT_SIZE`   | 3        | Кількість агентів у турнірному відборі |

---

## 🧮 Висновки

* Генетичний алгоритм дозволяє ефективно **моделювати еволюцію стратегій** у простих ігрових середовищах.
* Алгоритм демонструє, як **еволюційні принципи** можуть бути використані для **пошуку оптимальних стратегій поведінки** навіть у стохастичних середовищах.

---

## 📊 Приклад запуску

Після завершення роботи скрипт побудує два графіки:

* Еволюція середнього fitness
* Еволюція середньої стратегії популяції

🧩 Приклад консольного виводу
<img width="1325" height="335" alt="image" src="https://github.com/user-attachments/assets/bca94927-c8a5-43df-969b-effb5d856cc7" />


